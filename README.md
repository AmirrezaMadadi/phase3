📝 ToDo Manager – Project Overview

یک سامانه مدیریت وظایف و پروژه‌ها ساخته‌شده با FastAPI،
پشتیبانی‌شده توسط SQLAlchemy و پایگاه‌داده PostgreSQL.

این پروژه برای استفاده‌ی توسعه‌دهندگان، تیم‌ها، و افرادی طراحی شده که می‌خواهند یک سرویس تمیز، توسعه‌پذیر و استاندارد برای مدیریت تسک‌ها داشته باشند.

🚫 وضعیت رابط خط فرمان (CLI)

از نسخهٔ 0.3.x به بعد، استفاده از API تحت وب به عنوان روش اصلی تعامل پیشنهاد می‌شود.
امکان استفاده از CLI هنوز وجود دارد، اما غیرفعال اعلام شده و احتمال حذف آن در نسخه‌های آینده وجود دارد.

✨ قابلیت‌ها
🔹 نسخه 1 – معماری اولیه و ذخیره‌سازی در حافظه

(فقط برای توسعه اولیه – حذف شده از چرخه اصلی)

🔹 نسخه 2 – پایگاه‌داده PostgreSQL + رابط CLI

(غیرفعال‌شده، ولی همچنان قابل استفاده برای اهداف تست)

🔹 نسخه 3 – API کامل REST با FastAPI

نسخه فعال و استاندارد کنونی پروژه

مدیریت پروژه‌ها و وظایف

مسیرهای API کامل همراه با اعتبارسنجی

مستندسازی خودکار با Swagger (OpenAPI)

ساختار ماژولار و توسعه‌پذیر

📦 پیش‌نیازها

Python 3.11 یا بالاتر

PostgreSQL

Poetry (برای مدیریت وابستگی‌ها)
